*** Begin Patch
*** Update File: C:\Users\armad\OneDrive\Документы\Проект в разработке\S-Tracker\scripts\report-tend-ui-bundle.mjs
@@
-const f33Result = { raw: 936175, gzip: 288864 };
-const f34MoveDialogBudget = { raw: 24 * 1024, gzip: 8 * 1024 };
+const f34Result = { raw: 937481, gzip: 289178 };
+const f35PresetDialogBudget = { raw: 24 * 1024, gzip: 8 * 1024 };
@@
-  rawBytes <= f33Result.raw + f34MoveDialogBudget.raw,
-  `JavaScript bundle exceeds the reviewed F-34 move-dialog budget: ${rawBytes} bytes`,
+  rawBytes <= f34Result.raw + f35PresetDialogBudget.raw,
+  `JavaScript bundle exceeds the reviewed F-35 preset-dialog budget: ${rawBytes} bytes`,
@@
-  gzipBytes <= f33Result.gzip + f34MoveDialogBudget.gzip,
-  `Gzipped JavaScript bundle exceeds the reviewed F-34 move-dialog budget: ${gzipBytes} bytes`,
+  gzipBytes <= f34Result.gzip + f35PresetDialogBudget.gzip,
+  `Gzipped JavaScript bundle exceeds the reviewed F-35 preset-dialog budget: ${gzipBytes} bytes`,
@@
 assert.match(bundle.toString('utf8'), /tend-ui-move-dialog-confirm/);
+assert.match(bundle.toString('utf8'), /tend-ui-preset-name-input/);
+assert.match(bundle.toString('utf8'), /tend-ui-preset-dialog-close/);
+assert.match(bundle.toString('utf8'), /tend-ui-preset-dialog-cancel/);
+assert.match(bundle.toString('utf8'), /tend-ui-preset-dialog-confirm/);
@@
-console.log('Budget: F-33 result plus 24 KiB raw / 8 KiB gzip for Tend UI move dialog controls');
+console.log('Budget: F-34 result plus 24 KiB raw / 8 KiB gzip for Tend UI preset dialog controls');
*** End Patch