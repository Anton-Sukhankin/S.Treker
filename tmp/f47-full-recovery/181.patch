*** Begin Patch
*** Update File: ../S-Tracker/docs/tend-ui-integration.md
@@
-The next group is F-36: migrate the preset trigger and Save action while preserving dropdown selection/deletion, active preset, dirty-state and disabled behavior.
+## F-36 Preset Toolbar Result
+
+- the preset trigger and Save action render through Tend UI `Button` and `ChevronDown`;
+- React owns presentation and synchronized label/expanded/disabled state only;
+- vanilla code owns dropdown visibility, preset data, dirty comparison, replay, deletion and dialog opening;
+- initial/dirty state, creation, base/saved replay and active deletion pass;
+- all build, security and bundle gates remain green.
+
+The next group is F-37: migrate the column-settings footer actions while preserving library expansion, default reset, draft commit and Apply disabled behavior.
*** End Patch