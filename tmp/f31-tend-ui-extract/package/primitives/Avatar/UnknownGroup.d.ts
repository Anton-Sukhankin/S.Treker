import React from 'react';
import { AvatarSize } from './types';
type UnknownGroupProps = {
    size: AvatarSize;
};
export declare const UnknownGroup: (props: UnknownGroupProps) => React.JSX.Element;
export {};
