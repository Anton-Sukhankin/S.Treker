import React from 'react';
import { StepsProps } from './types';
declare const Steps: {
    ({ labelPlacement, direction, variant, ...props }: StepsProps): React.JSX.Element;
    displayName: string;
};
export { Steps };
