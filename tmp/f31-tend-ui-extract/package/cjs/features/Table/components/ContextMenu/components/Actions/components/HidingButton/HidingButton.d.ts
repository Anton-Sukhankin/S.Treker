import React from 'react';
import { HidingButtonProps } from './types';
declare const HidingButton: {
    ({ onClick, disabled }: HidingButtonProps): React.JSX.Element;
    displayName: string;
};
export { HidingButton };
