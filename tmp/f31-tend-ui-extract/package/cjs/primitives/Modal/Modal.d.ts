import React from 'react';
import { ModalProps } from './types';
declare const Modal: {
    ({ footer, okButtonProps, cancelButtonProps, okText, cancelText, scroll, size, confirmLoading, closeIconTooltip, title, children, centered, style, bodyStyle, width, styles, ...props }: ModalProps): React.JSX.Element;
    displayName: string;
};
export { Modal };
