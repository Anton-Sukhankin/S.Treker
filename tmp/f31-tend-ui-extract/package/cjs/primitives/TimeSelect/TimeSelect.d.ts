import React from 'react';
import { TimeSelectProps } from './types';
declare const TimeSelect: {
    ({ virtual, onChange, from, to, step, ...props }: TimeSelectProps): React.JSX.Element;
    displayName: string;
};
export { TimeSelect };
