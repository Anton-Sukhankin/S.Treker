import React from 'react';
import { CheckboxGroupSearchProps } from './types';
declare const CheckboxGroupSearch: {
    ({ optionAfter, optionDescription, placeholder, allowClear, showSearch, virtual, error, loading, filterOption, filterOptionProp, scrollable, optionRender, options, onSearch, onChange, value, onScroll, ...props }: CheckboxGroupSearchProps): React.JSX.Element;
    displayName: string;
};
export { CheckboxGroupSearch };
