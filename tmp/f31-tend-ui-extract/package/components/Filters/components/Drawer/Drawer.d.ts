import React from 'react';
import { DrawerProps } from './types';
declare const Drawer: {
    ({ loading, title, children, ...props }: DrawerProps): React.JSX.Element;
    displayName: string;
};
export { Drawer };
