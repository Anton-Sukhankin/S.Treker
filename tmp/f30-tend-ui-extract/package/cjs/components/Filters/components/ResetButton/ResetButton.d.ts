import React from 'react';
import { ResetButtonProps } from './types';
declare const ResetButton: {
    ({ filter, ...props }: ResetButtonProps): React.JSX.Element;
    displayName: string;
};
export { ResetButton };
