import React from 'react';
import { RadioGroupSearchProps } from './types';
declare const RadioGroupSearch: {
    ({ optionAfter, optionDescription, placeholder, allowClear, virtual, error, loading, filterOption, filterOptionProp, showSearch, scrollable, optionRender, options, onSearch, onScroll, ...props }: RadioGroupSearchProps): React.JSX.Element;
    displayName: string;
};
export { RadioGroupSearch };
