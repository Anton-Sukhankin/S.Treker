import React from 'react';
import { SortersButtonProps } from './types';
declare const SortersButton: {
    ({ tooltip, ...props }: SortersButtonProps): React.JSX.Element;
    displayName: string;
};
export { SortersButton };
