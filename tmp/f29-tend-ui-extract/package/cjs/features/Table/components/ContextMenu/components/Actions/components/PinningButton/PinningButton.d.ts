import React from 'react';
import { PinningButtonProps } from './types';
declare const PinningButton: {
    ({ pinned, onChange, onClick, disabled }: PinningButtonProps): React.JSX.Element;
    displayName: string;
};
export { PinningButton };
