import React from 'react';
import { TriggerProps } from './types';
declare const Trigger: {
    ({ children, onClick, ...props }: TriggerProps): React.JSX.Element;
    displayName: string;
};
export { Trigger };
