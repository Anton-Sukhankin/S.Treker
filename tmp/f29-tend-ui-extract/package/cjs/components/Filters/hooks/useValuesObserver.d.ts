import { FormInstance } from '../../../components/Form';
import { GenericObject } from '../../../types';
export declare const useValuesObserver: <T extends GenericObject = GenericObject>(name: string, form: FormInstance<T>, scope?: string) => any;
