import React from 'react';
import { AsyncCheckboxFilterProps } from './types';
declare const AsyncCheckboxFilter: {
    (props: AsyncCheckboxFilterProps): React.JSX.Element;
    displayName: string;
};
export { AsyncCheckboxFilter };
