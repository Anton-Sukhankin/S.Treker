import React from 'react';
import { SupportProps } from './types';
declare const Support: {
    ({ component: Component, href }: SupportProps): React.JSX.Element;
    displayName: string;
};
export { Support };
