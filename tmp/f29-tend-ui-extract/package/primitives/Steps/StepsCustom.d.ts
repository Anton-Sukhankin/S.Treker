import React from 'react';
import { StepsProps } from './types';
export declare const StepsCustom: {
    ({ items, direction, current, onChange, variant, ...props }: StepsProps): React.JSX.Element;
    displayName: string;
};
