import React from 'react';
import { AuthorizationProps } from './types';
declare const Authorization: {
    ({ signinButtonProps, signupButtonProps, onSignin, onSignup, }: AuthorizationProps): React.JSX.Element;
    displayName: string;
};
export { Authorization };
