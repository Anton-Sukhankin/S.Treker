import React from 'react';
import { Logo as Logo$1 } from '../../../../../../components/Logo/Logo.js';

const Logo = (props) => {
    return React.createElement(Logo$1, Object.assign({}, props));
};
Logo.displayName = 'Layout.Header.Logo';

export { Logo };
