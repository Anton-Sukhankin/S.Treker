import React from 'react';
import { RootProps } from './types';
declare const Root: {
    ({ profile, authenticated, stand, ...props }: RootProps): React.JSX.Element;
    displayName: string;
};
export { Root };
