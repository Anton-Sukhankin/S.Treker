import React from 'react';
import { CheckboxFilterProps } from './types';
declare const CheckboxFilter: {
    (props: CheckboxFilterProps): React.JSX.Element;
    displayName: string;
};
export { CheckboxFilter };
