import React from 'react';
import { ResetButtonProps } from './types';
declare const ResetButton: {
    ({ children, ...props }: ResetButtonProps): React.JSX.Element;
    displayName: string;
};
export { ResetButton };
